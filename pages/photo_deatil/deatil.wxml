<view class="container">

  <view class="bg_img">
    <image src="{{bg_img_url}}" class="photo_image_style" title="{{bg_img_name}}" mode="widthFix" />
  </view>

  <view class="bottom_msg">已有
    <text style="color:red">{{totalCount}}</text>条好友评论，你也评论两句吧，谢谢</view>
  <view class="chat_list" wx:for="{{photoCommentList}}" wx:key="comment">
    <view class="chat_face">
      <image src="{{item.photoUrl}}" />
    </view>
    <view class="chat_words">
      <view class="chat_meta">
        <text class="chat_user">{{item.nickName}}</text>
        <text class="chat_date">{{item.createtimestr}}</text>
      </view>
      <view>{{item.content}}</view>
    </view>
  </view>
  <view wx:if="{{bottom_line}}">
    <view class="bottom_line"></view>
  </view>
  <view class="bottom_msg">{{bottom_msg}}</view>
  <view class="send_msg">
    <form bindreset="photoComment">
      <input placeholder="你也评论两句吧，谢谢" class="send_ipt" bindinput="bindKeyInput" />
      <button form-type="reset" class="send_btn">发布</button>
    </form>
  </view>
  <view wx:if="{{open}}" class="doommview">
    <block wx:for="{{photoCommentList}}" wx:key="id">
      <text  class="aon" style="animation: first {{item.time}}s linear forwards;top:{{item.top}}%;color:{{item.color}};">
            {{item.nickName}}:{{item.content}}
        </text>
    </block>
  </view>
</view>